[project]
name = "drm-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tanu Shree", email = "tanushree.wrk@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "python-dotenv>=1.2.3",
    "python-multipart>=0.0.32",
    "strands-agents[openai]>=1.54.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
drm-agent = "drm_agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
